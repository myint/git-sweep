language: python

python:
    - "2.6"
    - "2.7"

install:
    - if [ "$TRAVIS_PYTHON_VERSION" == "2.6" ]; then pip --quiet install argparse; fi
    - pip --quiet install nose mock
    - python setup.py --quiet install

script:
    - python -m gitsweep.scripts.test
